# 复制为 .env 后按环境改。npm run dev 会读取（Vite 读 VITE_ 前缀的；其余由 server/dev.mjs 加载给 Token 代理）。

# ── 本地 Token 代理（本仓库自带：server/token-server.js）──────────────────────
# 默认 3100（桌面版 demo 用 3000，两个一起跑不撞；真撞了会自动往后找端口）
# TOKEN_PORT=3100

# 页面把会话 / 坐席状态请求打到同源 /get-session、/set-agent-status，
# 由 Vite 转给这个地址。默认就是本地代理，npm run dev 会自动设置；
# 换成你们自己的后端时改这里（地址可以是 https://你们的网关）。
# TOKEN_PROXY_ORIGIN=http://127.0.0.1:3100

# 平台接口路径，默认就是下面三个；环境不同可改
# CC_SEAT_ACCOUNT_PATH=/openapi/token/v1/seat/account/get
# CC_SEAT_STATUS_PATH=/openapi/token/v1/seats/set-status
# CC_SIP_WS_PATH=/api/fs/sip-ws
# CC_FS_TOKEN_PATH=/openapi/v1/token/fs
# 软电话 WSS 上要不要挂 fs token（默认 1 = 挂）。参考页（能打通外呼的那套）不挂，试 0。
# CC_SIP_WS_TOKEN=0

# ── 真机调试 ──────────────────────────────────────────────────────────────────
# 手机通过局域网 IP 打开页面时，必须 HTTPS 才拿得到麦克风（安全上下文要求）。
#   H5_HTTPS=1 npm run dev     → 自签名证书，手机首次打开要手动信任一次
#   也可以用内网穿透（ngrok / cloudflared）拿一个 https 域名指向 5174
# H5_HTTPS=1

# ── 会话（服务端拼好：token/fs + seat/account/get，与桌面 demo 一致）────────────
# 页面请求会话 / 坐席状态的地址：留空＝同源（推荐，由 dev 代理或线上反代转给后端）；
# 填了＝原样使用，例如跨域直连你们自己的后端（需要对方开 CORS）。
# VITE_SESSION_API=/your/session/path
# VITE_AGENT_STATUS_API=/your/status/path

# ── SIP 保活（可选覆盖）───────────────────────────────────────────────────────
# 默认 600 秒＝不额外发心跳，只由 JsSIP 在注册到期前续一次。
# 链路上有 nginx/NAT 空闲超时（nginx 默认 60 秒）时设 25 恢复心跳；设 0 关闭。
# VITE_SIP_KEEPALIVE=600

# ── 交付时可以按客户注入默认 API 主机（不设则留空，用户在设置里填）──────────────
# VITE_API_HOST=https://客户的接口网关
